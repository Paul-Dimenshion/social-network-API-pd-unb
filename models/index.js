const User = require('./User'); // imports the User model from the ./User module.
const Thought = require('./Thought'); // imports the Thought model from the ./Thought module.

module.exports = { User, Thought }; //  exports an object containing the User and Thought models for use in other modules.